@page "/account/sign-in"

<PageTitle>@Localizer["Войти"] - Reviews of everything</PageTitle>

<div class="center-block">
    <EditForm Model="@_model" OnValidSubmit="OnValidSubmitAsync">
        <DataAnnotationsValidator />

        <MudPaper Class="mx-3 px-5 py-3 text-center" Style="display: flex; flex-direction: column; align-items: center; width: 350px;">
            <MudText Class="mt-2" Style="font-size: 21px;">@Localizer["ВОЙТИ В УЧЕТНУЮ ЗАПИСЬ"]</MudText>
            <SocialNetworkSignIn />
            <div class="mt-3 w-100">
                <MudTextField FullWidth="true" @bind-Value="_model.Email" For="@(() => _model.Email)" Margin="Margin.Dense" Variant="Variant.Outlined" Placeholder="Email" InputType="InputType.Email"></MudTextField>
                <span class="my-2"></span>
                <MudTextField FullWidth="true" @bind-Value="_model.Password" For="@(() => _model.Password)" Margin="Margin.Dense" Variant="Variant.Outlined" Placeholder="@Localizer["Пароль"]" InputType="InputType.Password"></MudTextField>
            </div>

            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Class="mt-5" FullWidth="true" Color="Color.Dark">@Localizer["Войти"]</MudButton>
            <MudText Class="my-3" Style="font-size: 12px;">@Localizer["Нет учетной записи?"] <MudLink Style="font-size: 12px;" Href="./account/sign-up">@Localizer["Создать аккаунт"]</MudLink></MudText>

            <MudOverlay Visible="_sendRequest" DarkBackground="true" Absolute="true">
                <MudProgressCircular Color="Color.Secondary" Indeterminate="true" />
            </MudOverlay>
        </MudPaper>
    </EditForm>
</div>